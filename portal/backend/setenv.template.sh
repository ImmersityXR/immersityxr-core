#!/bin/sh

export MYSQL_DATABASE=
export MYSQL_USER=
export MYSQL_PASSWORD=
export MYSQL_ROOT_PASSWORD=

# To use this file, fill out the values above. 
# Then, copy this file to `setenv.sh` in the same directory.
# Finally, source the environment variables by using `. setenv.sh`.
