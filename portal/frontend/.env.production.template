VUE_APP_API_BASE_URL=
VUE_APP_VR_CLIENT_BASE_URL=
VUE_APP_RELAY_BASE_URL=
VUE_APP_RTC_URL=
# Shared secret the relay server requires from VR clients
# (config.auth.clientSecret in the relay). Leave empty if the relay
# does not enforce client authentication.
VUE_APP_VR_AUTH_TOKEN=
